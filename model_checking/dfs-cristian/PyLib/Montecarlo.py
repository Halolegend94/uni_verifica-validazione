class Montecarlo

    def __init(self, sigma, delta):
        self.sigma = sigma
        self.delta = delta
        self.actions
        return

    def verify(self, modelName, input_vars, limits):
        #compute number of trials
        M = roof(log(delta) / log(1 - self.sigma))
        #compute random disturbance sequence
        for i in range(M):
            
